id: F002
function: ScreenHeader
range:
- 66
- 72
args:
- char Title[MAX_STRING_SIZE]
return: void
requirement:
  trigger: "画面をクリアし、操作画面に見出しを表示したいとき"
  action: "コンソール画面をクリアし、引数で与えられたタイトル文字列を目立つ枠で囲んで表示する"
  effect: "画面に見出しが強調され、処理の開始や区切りが明確になる"
  constraint: "Windows環境で 'cls' コマンドが有効である必要がある"
