id: F005
function: ReadTime
range:
- 94
- 100
args:
- char Message[MAX_STRING_SIZE]
return: stTime
requirement:
  trigger: "ユーザーに時間（時・分）の入力を求める必要があるとき"
  action: "指定されたメッセージを表示し、時刻（時・分）を標準入力から読み取って構造体に格納する"
  effect: "ユーザーが入力した時刻情報を1つの構造体として取得できる"
  constraint: "時間の範囲（例：0〜23時など）の妥当性はこの関数内では検証されない"
