function: DeleteUserByID
id: F016
taec:
  action: リンクリストを線形探索して、指定されたIDを持つノードを見つけて削除する。
  constraint: 削除対象のユーザーが見つからない場合はリストに変更がなく、メモリ解放時にNULLポインタを渡さないようにする。
  effect: 指定されたIDを持つユーザーがリストから削除され、メモリが解放される。
  trigger: 指定されたIDを持つユーザーを削除する要求が発生したとき。
