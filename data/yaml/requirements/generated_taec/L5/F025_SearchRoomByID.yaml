function: SearchRoomByID
id: F025
taec:
  action: リストの各要素を順にチェックし、IDが一致する要素を見つけたらそのポインタを返します。
  constraint: IDが見つからない場合はNULLが返されるため、呼び出し側でNULLチェックを行う必要があります。
  effect: 一致するIDが見つかればその部屋のポインタを返し、見つからなければNULLを返します。
  trigger: リンクリストから特定のIDを持つ部屋を検索する必要があるときに関数が呼び出されます。
