function: SwapReservationData
id: F045
taec:
  action: 一時変数を使用して、2つの予約構造体の各フィールドの値を交換する。
  constraint: 予約データはReservationPtr型のポインタでなければならない。
  effect: 2つの予約データの内容が入れ替わる。
  trigger: 2つの予約データを入れ替える必要があるときに関数が呼び出される。
