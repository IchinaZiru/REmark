function: SearchReservationByID
id: F037
taec:
  action: リストの先頭から順に各予約のIDを比較する。
  constraint: リストの終端に達しても一致するIDが見つからなければ、NULLを返す。
  effect: 一致するIDの予約が見つかれば、その予約のポインタを返す。
  trigger: 予約リストから特定のIDの予約を検索する必要があるとき。
