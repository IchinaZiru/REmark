function: IsDate1BeforeDate2
id: F006
taec:
  action: 年、月、日の順に2つの日付を比較し、最初に異なる値を持つ単位で判定を行う。
  constraint: 入力データの妥当性は呼び出し元で確認する必要があり、関数内でのエラー処理は行わない。
  effect: 最初の日付が2番目の日付より前の場合はtrueを返し、それ以外の場合はfalseを返す。
  trigger: 2つの日付が与えられたときに、最初の日付が2番目の日付よりも前かを確認する必要がある。
