function: ReadTime
id: F005
taec:
  action: 関数はメッセージを表示し、ユーザーから時と分を標準入力で取得します。
  constraint: 入力が不正な場合のエラーチェックは行われず、不正なデータが構造体に格納される可能性があります。
  effect: 取得した時と分がstTime構造体に格納され、関数から返されます。
  trigger: ユーザーが時間を入力する必要があるときに関数が呼び出されます。
