function: ReadDate
id: F004
taec:
  action: 関数は指定されたメッセージを表示し、ユーザーから年、月、日を入力として受け取る。
  constraint: ユーザーは年、月、日を整数として入力しなければならない。
  effect: 入力された日付がstDate構造体に格納され、関数から返される。
  trigger: ユーザーが日付を入力する必要があるときに関数が呼び出される。
