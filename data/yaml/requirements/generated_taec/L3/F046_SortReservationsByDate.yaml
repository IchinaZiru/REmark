function: SortReservationsByDate
id: F046
taec:
  action: リスト内の各予約を二重ループで比較し、必要に応じてデータを入れ替えます。
  constraint: 各予約のデータを比較し、日付が前のものを先にするようにデータを交換します。
  effect: 予約リストが日付順にソートされます。
  trigger: 予約リストが日付順にソートされていないときにこの関数が呼び出されます。
