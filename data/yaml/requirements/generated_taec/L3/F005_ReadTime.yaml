function: ReadTime
id: F005
taec:
  action: 関数はメッセージを表示し、ユーザーから時と分を標準入力で取得する。
  constraint: ユーザーは時と分を整数として正しく入力する必要がある。
  effect: 取得した時と分がstTime構造体に格納され、関数の戻り値として返される。
  trigger: ユーザーが時間を入力する必要があるときに関数が呼び出される。
