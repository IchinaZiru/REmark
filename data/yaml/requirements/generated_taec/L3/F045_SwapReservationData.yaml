function: SwapReservationData
id: F045
taec:
  action: 一時変数を使用して、2つの予約構造体の各フィールドの値を交換する。
  constraint: Reservation1とReservation2の各フィールド（ID、UserID、RoomID、Date、Start_Time、End_Time）を正確に入れ替える必要がある。
  effect: Reservation1とReservation2の各フィールドの値が入れ替わる。
  trigger: 2つの予約データを入れ替える必要があるときに関数が呼び出される。
