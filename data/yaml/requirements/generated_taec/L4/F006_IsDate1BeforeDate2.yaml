function: IsDate1BeforeDate2
id: F006
taec:
  action: 最初の日付と2番目の日付を年、月、日の順に比較する。
  constraint: 年、月、日がすべて同じか、最初の日付が2番目の日付より後の場合はfalseを返す。
  effect: 最初の日付が2番目の日付より前である場合にtrueを返す。
  trigger: 2つの日付が与えられたときに関数が呼び出される。
