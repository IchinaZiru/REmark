function: SearchUserByID
id: F014
taec:
  action: リストの先頭から順に各ユーザーのIDを比較する。
  constraint: リストの終端までに一致するユーザーが見つからない場合はNULLを返す。
  effect: 一致するユーザーが見つかればそのユーザーへのポインタを返し、見つからなければNULLを返す。
  trigger: ユーザーリストから特定のIDを持つユーザーを検索する要求があるとき。
