function: ReadDate
id: F004
taec:
  action: 関数は指定されたメッセージを表示し、ユーザーから年、月、日を入力として受け取ります。
  constraint: ユーザーの入力が不正な場合のエラーハンドリングは実装されていません。
  effect: 入力された日付がstDate構造体に格納され、関数から返されます。
  trigger: ユーザーが日付を入力する必要があるときに関数が呼び出されます。
