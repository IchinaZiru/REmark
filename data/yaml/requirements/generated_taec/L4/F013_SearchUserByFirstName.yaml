function: SearchUserByFirstName
id: F013
taec:
  action: ユーザーリストを線形に探索し、指定された名前と一致するユーザーを探す。
  constraint: 一致するユーザーが見つからない場合、何も表示されず、フラグは変更されない。
  effect: 一致するユーザーが見つかった場合、そのユーザー情報を表示し、フラグをtrueに設定する。
  trigger: ユーザーリストの検索が開始されるとき。
