function: DeleteUserByID
id: F016
taec:
  action: リンクリストを線形探索して、指定されたIDを持つノードを見つけて削除する。
  constraint: 指定されたIDを持つユーザーが見つからない場合、リストに変更はない。
  effect: 指定されたIDを持つユーザーが見つかった場合、そのノードがリストから削除され、メモリが解放される。
  trigger: 指定されたIDを持つユーザーを削除する要求が発生したとき。
