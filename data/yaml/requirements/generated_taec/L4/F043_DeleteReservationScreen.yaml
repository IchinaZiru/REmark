function: DeleteReservationScreen
id: F043
taec:
  action: 入力された予約IDに基づいて予約を検索し、ユーザーに削除の確認を求めます。
  constraint: 予約IDが存在しない場合はエラーメッセージが表示され、削除は行われません。
  effect: 予約が削除されると、対応する部屋の予約状態が更新され、成功メッセージが表示されます。
  trigger: ユーザーが予約IDを入力することによって処理が開始されます。
