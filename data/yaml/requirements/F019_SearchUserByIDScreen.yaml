id: F019
function: SearchUserByIDScreen
range:
- 315
- 330
args:
- UserPtr UserList
return: void
requirement:
  trigger: "ユーザーIDによる検索を行う画面が呼び出されたとき"
  action: "IDを入力させてリストから一致するユーザーを検索し、結果を表示する"
  effect: "該当するユーザーが存在すれば情報が表示され、存在しなければ『User not Found』と表示される"
  constraint: "入力IDは 0〜9999 の範囲に限定され、それ以外は再入力が求められる"

