id: F025
function: SearchRoomByID
range:
- 459
- 470
args:
- RoomPtr RoomList
- int ID
return: RoomPtr
requirement:
  trigger: "部屋IDを指定して検索したいとき"
  action: "指定されたIDと一致する部屋を線形探索し、見つかればそのポインタを返す"
  effect: "IDで特定された部屋情報にアクセスできるようになる"
  constraint: "一致するIDが存在しない場合はNULLを返し、探索は全件走査で行う"

